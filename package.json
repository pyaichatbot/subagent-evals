{
  "name": "subagent-evals-monorepo",
  "private": true,
  "version": "0.5.0",
  "author": "Praveen Yellamaraju",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "build:pages": "pnpm build && node apps/hosted/generate.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "clean": "rm -rf packages/*/dist packages/*/.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "hyperframes": "^0.4.10",
    "tsx": "4.21.0",
    "typescript": "6.0.3",
    "vitest": "4.1.4"
  }
}
