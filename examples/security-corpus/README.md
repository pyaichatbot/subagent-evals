# Security corpus examples

These files are example corpus packs for the trust-roadmap MVP.

They are documentation fixtures for:

- prompt injection
- jailbreak
- indirect injection
- data exfiltration
- path traversal
- hostile diff and unicode attack cases

They are meant to show the target pack shape and case organization, not to claim that every field is already enforced by the current engine build.
