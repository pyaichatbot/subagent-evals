id: planner-replay
agent: planner
kind: runtime
input:
  task: "Plan work for adding retry handling"
  fixtures:
    output_text: "{\"summary\":\"add retry\",\"subtasks\":[{\"id\":\"t1\"}]}"
expected:
  score_min: 0.5
  assertions:
    - type: contains
      value: "summary"
    - type: contains
      value: "subtasks"
